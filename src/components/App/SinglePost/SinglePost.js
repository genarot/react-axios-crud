import React from 'react';
import cssModule from 'react-css-modules';
import styles from './SinglePost.css';

const SinglePost = () => (
  <div>
    SinglePost
  </div>
);

SinglePost.propTypes = {

};

export default cssModule(SinglePost, styles);
